#ifndef __STATUS_H
#define __STATUS_H

#include <iostream>
using namespace std;

#include "date.h"
#include "time.h"

class Status {
    const Date theDate;
    const Time theTime;
    char* text;

    //add c'tor, d'tor
    //we need to choose if we want to delete the copy c'tor

};


#endif //__STATUS_H